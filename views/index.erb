<meta charset="UTF-8">

<form action="/register" method="post">
  <input type="submit" value="Register"
         name="Submit" id="frm1_submit" />
</form>

<h1>Библиотеки</h1>

<% if flash[:success] %>
    <p class="bg-success"><%= flash[:success] %></p>
<% end %>

<% if flash[:error] %>
    <p class="bg-danger"><%= flash[:error] %></p>
<% end %>

<% if @libraries.empty? %>
    В момента няма налични книги
<% end %>
<% @libraries.each do |library| %>
    <p>
    <form method="get" action="/libraries?id=">
      <h3><a href="/libraries?id=<%= library.id %>"><%= library.name %></a></h3>
    </form>
    </p>
<% end %>


<h2>Добави библиотека</h2>
<form method="post" action="/libraries">

  <div class="form-group">
    <label for="text-input">Име</label>
    <textarea id="text-input"
              class="form-control"
              name="name"
              placeholder="Име"></textarea>
    <label for="text-input">Адрес</label>
    <textarea id="text-input"
              class="form-control"
              name="address"
              placeholder="Адрес"></textarea>
  </div>
  <button type="submit" class="btn btn-default">Добави</button>
</form>
