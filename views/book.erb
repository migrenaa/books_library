<meta charset="UTF-8">

<form method="get" action="/books/:id">
    <label for="text-input">Име</label>
    <% @book.title %></form>
    <label for="text-input">Автор</label>
    <%	 @book.author %>
    <label for="text-input">Жанр</label>
    <% @book.genre %>
    <label for="text-input">Описание</label>
    <% @book.description %>
</form>
<form method="delete" action="/books/:id">
<button type="submit" class="btn btn-default">Изтрий</button>
</form>
